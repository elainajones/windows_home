# The following is an example only of the expected format
# for the .git-credential file. This is where a personal access token
# is used for situations where SSH key authentication is not permitted
# by policy.
#https://<username>:<password>@<example.domain.com>
